<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Ingress (входящие запросы к hub'у)
    |--------------------------------------------------------------------------
    |
    | Параметры защиты эндпоинта POST /api/notifications.
    |
    */

    'ingress' => [
        // Секрет, который обязан передать клиент в заголовке X-Notification-Hub-Token
        // (или Authorization: Bearer <token>, или ?token=...).
        'token' => env('NOTIFICATION_HUB_INGRESS_TOKEN', ''),

        // Опциональный allowlist IP/CIDR, разделённых запятыми.
        // Пусто => проверка по IP отключена, действует только токен.
        'allowed_ips' => env('NOTIFICATION_HUB_ALLOWED_IPS', ''),
    ],

    /*
    |--------------------------------------------------------------------------
    | Telegram (исходящая отправка из hub'а)
    |--------------------------------------------------------------------------
    |
    | Креды бота, через которого hub шлёт сообщение в Telegram.
    |
    */

    'telegram' => [
        'token' => env('TELEGRAM_LOGGER_TOKEN', ''),
        'chat_id' => env('TELEGRAM_LOGGER_CHAT_ID', ''),
    ],

];
