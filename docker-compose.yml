services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: ${APP_NAME:-notification-gg-hub}-app
    working_dir: /var/www/html
    restart: unless-stopped
    volumes:
      - ./src:/var/www/html
    env_file:
      - .env
    environment:
      APP_ENV: ${APP_ENV:-local}
      PHP_MEMORY_LIMIT: ${PHP_MEMORY_LIMIT:-512M}
    depends_on:
      - db
    networks:
      - appnet

  nginx:
    image: nginx:latest
    container_name: ${APP_NAME:-notification-gg-hub}-nginx
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-8080}:80"
      - "${HTTPS_PORT:-8443}:443"
    volumes:
      - ./src:/var/www/html
      # Pick config via .env (e.g. default.conf / prod-http.conf / prod.conf)
      - ./docker/nginx/${NGINX_CONF:-default.conf}:/etc/nginx/conf.d/default.conf:ro
      - ./docker/certbot/www:/var/www/certbot
      - ./docker/certbot/conf:/etc/letsencrypt
    depends_on:
      - app
    networks:
      - appnet
    # Reload periodically so renewed certs are picked up (safe even for local).
    command: /bin/sh -c "while :; do sleep 6h; nginx -s reload; done & nginx -g 'daemon off;'"

  db:
    image: mysql:latest
    container_name: ${APP_NAME:-notification-gg-hub}-mysql
    restart: unless-stopped
    ports:
      - "${DB_PORT_FORWARD:-3306}:3306"
    env_file:
      - .env
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-app}
      MYSQL_USER: ${DB_USERNAME:-app}
      MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - appnet

  certbot:
    image: certbot/certbot:latest
    container_name: ${APP_NAME:-notification-gg-hub}-certbot
    restart: unless-stopped
    volumes:
      - ./docker/certbot/www:/var/www/certbot
      - ./docker/certbot/conf:/etc/letsencrypt
    # Renew certificates twice a day (Let's Encrypt recommendation).
    # Run a renew loop. We set a shell entrypoint because this image defaults to entrypoint "certbot".
    # For one-shot issuance use: `docker compose run --rm --entrypoint certbot certbot certonly ...`
    entrypoint: /bin/sh
    command: -c "trap exit TERM; while :; do certbot renew --webroot -w /var/www/certbot || echo \"[certbot] renew failed at $$(date -u +%FT%TZ)\"; sleep 12h & wait $${!}; done"

volumes:
  dbdata:

networks:
  appnet:
    driver: bridge
